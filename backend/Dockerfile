# Image de base générique en attendant le vrai code du backend
FROM python:3.12-slim

WORKDIR /app

# Placeholder : sera remplacé par les vraies dépendances une fois le code backend disponible
COPY . .

EXPOSE 8000

CMD ["python3", "-m", "http.server", "8000"]